from app.extraction.schema import MetricRecord, SourceChunk


def extract_metrics_with_rules(chunk: SourceChunk) -> list[MetricRecord]:
    """Placeholder for transparent regex/table rules before LLM fallback."""
    return []
